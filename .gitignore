# Secrets — never commit
.env
**/.env
!.env.example

# Go
backend/server
backend/server.exe
backend/server.log
backend/uploads/
*.exe

# Node / frontend
node_modules/
frontend/dist/
frontend/node_modules/
*.tsbuildinfo

# Local tool settings (machine-specific)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db

# Kubernetes / Rancher deployment manifests.
# Manifests are tracked (needed by the Jenkins pipeline), but the real Secret
# holding live credentials must NEVER be committed — recreate it in-cluster
# from rancher-yaml/secret.example.yml.
rancher-yaml/secret.yml
DEPLOYMENT_FIXES.md
frontend/docker-compose.yml

# Test scripts and reports
TEST-REPORT.md
test-fixes.sh
ws-*.mjs

# Load test outputs (results regenerated per run)
loadtest/loadtest-result.json
loadtest/loadtest-verdict.json
