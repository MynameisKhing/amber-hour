# Services are defined in deployment.yml (amber-backend and amber-frontend)
# This file is kept for reference only.

# For direct pod access without Ingress, you can expose services as NodePort:
# apiVersion: v1
# kind: Service
# metadata:
#   name: amber-frontend-nodeport
#   namespace: khing
# spec:
#   type: NodePort
#   selector:
#     app: amber-hour
#     component: frontend
#   ports:
#     - port: 80
#       targetPort: 80
#       nodePort: 30080  # Range: 30000-32767
